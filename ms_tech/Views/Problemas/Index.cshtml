@model IEnumerable<ms_tech.Models.Problemas>

@{
    ViewBag.Title = "Lista de Problemas";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("Nuevo Problema", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Productos.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Activo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Productos.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Activo)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id = item.IdProblema }) |
            @Html.ActionLink("Detalles", "Details", new { id = item.IdProblema }) |
            @Html.ActionLink("Borrar", "Delete", new { id = item.IdProblema })
        </td>
    </tr>
}

</table>
